---
globs: docs/**/*.md,docs/**/*.rst,*.md
---

# 文档编写规范

## 文档结构

项目文档位于 `docs/` 目录下:

- `installation.md` - 安装说明
- `tutorial.md` - 快速入门教程
- `data/` - 数据接口文档
- `contributing.md` - 贡献指南
- `changelog.md` - 版本更新日志

## Markdown 格式规范

### 标题层级

```markdown
# 一级标题 (项目/模块名称)
## 二级标题 (大类别)
### 三级标题 (具体接口或小节)
#### 四级标题 (详细说明)
```

### 代码块

使用三个反引号标注代码块,并指定语言:

```markdown
​```python
import akshare as ak

df = ak.stock_zh_a_hist(symbol="000001")
print(df)
​```
```

### 接口文档模板

```markdown
## 接口名称

### 接口: function_name

**目标地址**: https://data.example.com/

**描述**: 获取某某数据

**限量**: 单次返回所有历史数据

**输入参数**

| 参数名 | 类型 | 必选 | 默认值 | 描述 |
|--------|------|------|--------|------|
| symbol | str | Y | - | 股票代码 |
| start_date | str | N | "20200101" | 开始日期 |
| end_date | str | N | "20231231" | 结束日期 |

**输出参数**

| 名称 | 类型 | 描述 |
|------|------|------|
| 日期 | object | 交易日期 |
| 开盘 | float64 | 开盘价 |
| 收盘 | float64 | 收盘价 |
| 最高 | float64 | 最高价 |
| 最低 | float64 | 最低价 |
| 成交量 | float64 | 成交量 |

**接口示例**

​```python
import akshare as ak

# 获取数据
df = ak.function_name(symbol="000001", start_date="20200101")
print(df)
​```

**数据示例**

| 日期 | 开盘 | 收盘 | 最高 | 最低 | 成交量 |
|------|------|------|------|------|--------|
| 2020-01-02 | 10.50 | 10.60 | 10.65 | 10.45 | 1000000 |
| 2020-01-03 | 10.60 | 10.55 | 10.70 | 10.50 | 1100000 |
```

## reStructuredText (RST) 格式

对于 Sphinx 文档,使用 RST 格式:

```rst
接口名称
========

功能描述
--------

获取某某数据。

代码示例
--------

.. code-block:: python

   import akshare as ak
   
   df = ak.function_name()
   print(df)

数据示例
--------

.. code-block:: python

          日期    开盘    收盘
   0  2020-01-02  10.50  10.60
   1  2020-01-03  10.60  10.55
```

## README 更新

修改 [README.md](mdc:README.md) 时注意:

1. 保持徽章 (badges) 部分的准确性
2. 更新版本号和依赖要求
3. 示例代码应能实际运行
4. 中英文描述保持一致

## 更新日志规范

在 `docs/changelog.md` 中记录版本更新:

```markdown
### 版本 X.Y.Z

发布日期: YYYY-MM-DD

**新增**
- 新增 xxx 数据接口
- 新增 yyy 功能模块

**修复**
- 修复 xxx 接口返回数据错误
- 修复 yyy 参数验证问题

**优化**
- 优化 xxx 接口性能
- 更新文档说明
```

## 文档链接

- 使用相对路径引用项目内文件
- 外部链接使用完整 URL
- 确保所有链接在发布前经过验证

## 中文规范

1. 使用简体中文
2. 技术术语优先使用英文或中英文混合
3. 数字和单位之间加空格: "10 万" 而非 "10万"
4. 专有名词首字母大写: "Python", "AKShare"
